---
elastics:

  - url: ${ELASTIC_URL}
    username: ${ELASTIC_USERNAME}
    password: ${ELASTIC_PASSWORD}

actions:
  slack:
    url: https://domain.slack.com/webhook/spiffyness
  pagerduty:
    api-token: ${PAGERDUTY_TOKEN}
